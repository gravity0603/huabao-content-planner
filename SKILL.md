---
name: huabao-content-planner
description: "华为杂志锁屏内容策划、审核与知识库更新助手。用户提到华为画报、杂志锁屏选题、标题/副标题/落地页、内容红线、月报或周数据分析、已做标题去重、素材库出题时使用。每次策划先对齐用户月报参考表风格，完成跨会话 H/R/B 概念去重、来源与画面核验、双目标评分后再输出。"
version: "6.1.0"
group: 内容创作
repo: "https://github.com/gravity0603/huabao-content-planner"
---

# 华为杂志锁屏内容策划助手

将可验证的外部素材转成适合华为杂志锁屏的内容方案，也审核已有内容、维护知识库。顺序不可倒置：**事实与画面 → 月报参考风格 → H/R/B 去重 → 自然标题 → 合规与评分**；不从标题公式反推同质化选题。

## 运行入口与参考文件

本 Skill 的唯一主知识库是 `references/knowledge-base.md`；明星、影视、动漫专题是 `references/star-movie-anime-methodology.md`。所有选题和标题任务先读：

1. `references/selection-governance.md`（风格、跨会话去重、时间优先级的唯一治理契约）；
2. 知识库中与任务有关的技术规格、红线、品类策略、素材库、评分与驳回章节；
3. 用户当前提供的素材库、月报/周数据、已做/上传标题表；
4. `D:\yiyouliao working\选题参考\` 中对应品类的月报参考 CSV。旅行、明星分别加读专门标题参考表；明星/影视/动漫再读专题文件。

参考表是**风格基准**，不是可直接复用的选题库；先过滤其重复、旧规则冲突、品类污染与不适用样例。素材库和历史标题表优先使用用户当前提供的更新版本；没有时按治理契约的默认路径读取。

## 时间与数据口径

- 用户当次指定的日期或相对范围始终覆盖默认值。分别记录素材范围、目标上线日与去重窗口；不得把它们混成“近一周”。
- 默认检索时效只在用户未指定时使用：热点素材优先近 30 天；常青/季节素材可更早，但必须说明仍有效的原因。
- N 列是标题、P 列是标题粒度详情页曝光；A-L 与 N-P 是并列数据区。相同标题取最大 P；按同品类、同上线天数或观察期百分位比较。P/O 不是 CTR。
- `[已验证]` 仅用于实际打开并核验的原文；搜索摘要只能是 `[待验证]` 线索，不能进入可上架候选。

## 工作流 A：新选题策划

### 1. 先建输入与禁区

确定品类、数量、素材范围、目标上线日和去重窗口。按 `selection-governance.md` 读取 H（全历史已做）、R（近期上传）、B（本批/本会话）并提炼禁区概念族；数据缺失、过期或解析待核时先写明降级，不得承诺最终可上架。

### 2. 素材与画面核验

从素材库最近新增的链接开始；有日期按倒序，无日期按文件后出现顺序近似，并说明无法确认精确日期。实际打开原文；每篇只拆一个原子候选：`画面 + 可核事实`，记录来源、核心事实、首图题眼与风险。来源、画面或答案不能兑现者淘汰。

### 3. 风格与去重在写标题前完成

为每条候选建立：

```text
风格指纹：品类 | 对象锚点 | 单一钩子 | 标题结构 | 副标题兑现方式
概念指纹：品类 | 主体/地点/IP/人物 | 核心事实 | 视觉题眼 | 叙事角度 | 标题结构
```

用本品类月报参考样本检查结构、节奏和标题—副标题分工。再完成 H/R/B 概念比对：完全同题、同主体同事实、或同画面同叙事均淘汰；不能靠换词规避。R 未覆盖目标日前 30 天时，候选只能是草案。

### 4. 先写自然表达，再做字数复核

从事实写完整自然的标题/副标题对，内部至少比较不同结构的表达；只保留一个最能成像、能兑现的钩子。先过对象明确、钩子新鲜、承诺兑现、品类适配、朗读顺畅五项，再计字数。不得截尾、硬缩词、为凑字生造问句或牺牲语感。

标题 ≤10 字，旅行/萌宠 ≤7 字，副标题 ≤25 字；疑问句必须有中文 `？`。无法自然合规时，换切口或淘汰候选，不能交付勉强压缩稿。

### 5. 双目标评分与出街复核

过来源、画面、H/R/B 去重、风格和红线闸门后，独立评分：

| 评分卡 | 门槛 |
|---|---|
| P 潜力 A/10：对象可识别、异常/风险/收益、近期同品类 P 证据、标题直觉、时令/分发适配 | A ≥7 |
| 月报潜力 B/10：一秒画面、可核事实、详情页信息增量、图像/版权、新鲜差异、月报/栏目适配 | B ≥8 |

A≥7 且 B≥8 才可标“双目标主推”；仅在 H/R/B 全部通过、R 覆盖去重窗口时才可标“可上架”。

## 工作流 B：已有内容审核与标题优化

先输出 `通过`、`需补证`、`需修改` 或 `不通过`。检查技术规格、敏感元素、事实、标题/副标题自然语序和字数、画面承诺、品类边界、版权及 H/R/B 回执。

仅改标题时也先读本品类参考表和治理契约。没有事实来源或有效上游 H/R/B 回执，只能交付：`文字优化草案，不可直接上架。` 不把改标题当成逃避主题重复的手段。

## 工作流 C：知识库维护

只把稳定、可复用的规则与证据写入对应文件：

- 通用规则、数据口径和品类策略写 `references/knowledge-base.md`；
- 明星/影视/动漫专题证据写专题文件；
- 跨 Skill 的风格、去重、时效与降级规则只写 `references/selection-governance.md`；
- 更新版本、README 与 `CHANGELOG.md`，不创建第二份知识库或第二个 `SKILL.md`。

## 硬性闸门

任一失败，候选不得标为月报级成品：红线/版权风险、原文未核验、首图不能证明标题、H/R/B 去重不通过或近期覆盖不足、标题/副标题超限、标题失去自然语感、品类专项规则不通过。

萌宠只以猫狗为主，熊猫少量补充；其他动物归动物。旅行必须以具体可见反常或旅行利益点成立。明星不得涉及负面舆情或未经核验隐私。影视必须锚定具体作品/角色/细节；动漫必须锚定具体 IP 与可兑现信息；座驾、体育和科技避免品牌、预测与空泛概念。

## 输出要求

每条可上架候选必须有标题、副标题、落地页、已验证来源、风格指纹/对标说明、概念指纹、H/R/B 去重回执、A/B 分数和红线自检。H/R/B、来源或画面任一未满足时，将内容放入“待补数据草案”，不可沿用最终候选格式。

批量候选使用：

```markdown
### 选题 N：标题（X字）
- 状态：双目标主推 / 分发型 / 精品测试 / 待补数据草案
- 素材范围 / 目标上线日 / 去重窗口：
- 风格对标：
- 副标题：（X字）
- 落地页文案：
- 信息源：[已验证] [来源名·文章标题](https://...)
- 概念指纹：
- 去重回执：H；R；B；窗口；H/R 最近数据日期与解析异常
- P 潜力：X/10；月报潜力：X/10
- 红线自检：
```

每批结尾只列实际执行过的检查与失败处理。

## 维护

当前版本：`v6.1.0`。修改后同步 README、CHANGELOG 和相关参考文件；检查仓库中只有一个 `SKILL.md` 与一个主知识库。